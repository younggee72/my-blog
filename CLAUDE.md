# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code가 참고할 프로젝트 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어서 정적 블로그 웹사이트로 보여주는 프로젝트입니다.
글쓴이는 마크다운 파일만 작성하면 되고, 사이트는 이를 읽어 목록/본문 페이지로 렌더링합니다.

## 기술 스택 / 제약 조건

- **순수 HTML, CSS, JavaScript만 사용.** React, Vue, Svelte 등 프레임워크 금지.
- **빌드 도구/번들러 없음.** webpack, vite, npm 빌드 스크립트 등을 두지 않음. 브라우저에서 파일을 그대로 열거나 정적 서버로 서빙하면 동작해야 함.
- 마크다운 파싱은 브라우저에서 자바스크립트로 직접 처리 (직접 구현한 경량 파서 또는 별도 설치 없이 `<script>` 태그로 불러오는 CDN 라이브러리 정도만 허용). npm 의존성 설치가 필요한 패키지는 지양.
- 외부 CSS 프레임워크(Bootstrap, Tailwind 등) 사용하지 않음. 직접 CSS 작성.

## 디자인 원칙

- **깔끔하고 읽기 좋은 타이포그래피**: 본문 가독성 최우선 (적절한 줄 간격, 본문 폭 제한 약 65-75ch, 읽기 편한 폰트 크기).
- **다크 모드 지원**: `prefers-color-scheme` 미디어 쿼리 기반 자동 전환 + 수동 토글 버튼. 색상 값은 CSS 변수(`:root`, `[data-theme="dark"]`)로 관리.
- **모바일 반응형**: 모바일 퍼스트로 작성, 주요 브레이크포인트에서 레이아웃 확인 (좁은 화면에서 네비게이션/사이드바 등이 무너지지 않도록).
- 불필요한 장식/애니메이션 최소화, 콘텐츠(글) 중심의 미니멀한 UI.

## 예상 구조 (참고용, 실제 구현 시 조정 가능)

```
/posts/          마크다운 글 파일들 (.md)
/index.html      글 목록 페이지
/post.html       글 본문 페이지 (쿼리 파라미터 등으로 특정 글 로드)
/css/            스타일시트
/js/             마크다운 파서, 렌더링 로직, 다크모드 토글 등
```

## 작업 시 참고사항

- 새 기능을 추가할 때 프레임워크나 빌드 도구를 끌어오려는 유혹이 있다면 먼저 순수 JS로 가능한지 확인.
- 다크 모드 대응이 빠진 새 UI 요소를 추가하지 않도록 주의 (라이트/다크 모두 확인).
- 모바일 화면에서의 표시도 항상 함께 확인.

# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code가 참고할 프로젝트 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어서 정적 블로그 웹사이트로 보여주는 프로젝트입니다.
글쓴이는 마크다운 파일만 작성하면 되고, 사이트는 이를 읽어 목록/본문 페이지로 렌더링합니다.

## 기술 스택 / 제약 조건

- **순수 HTML, CSS, JavaScript만 사용.** React, Vue, Svelte 등 프레임워크 금지.
- **빌드 도구/번들러 없음.** webpack, vite, npm 빌드 스크립트 등을 두지 않음. 브라우저에서 파일을 그대로 열거나 정적 서버로 서빙하면 동작해야 함.
- 마크다운 파싱은 브라우저에서 자바스크립트로 직접 처리 (직접 구현한 경량 파서 또는 별도 설치 없이 `<script>` 태그로 불러오는 CDN 라이브러리 정도만 허용). npm 의존성 설치가 필요한 패키지는 지양.
- 외부 CSS 프레임워크(Bootstrap, Tailwind 등) 사용하지 않음. 직접 CSS 작성.

## 디자인 원칙

- **다크 네온(사이버펑크) 테마가 기본이자 유일한 색상 정체성**: 배경은 항상 검은색(`--bg` 계열), 강조 요소(제목, 링크, 테두리, 구분선)는 네온 시안(`--neon-cyan`)/마젠타(`--neon-magenta`)로 글로우(`text-shadow`/`box-shadow`) 처리. 라이트 모드는 없음 — `prefers-color-scheme` 분기 불필요.
- **본문 텍스트는 가독성 우선**: 네온 색은 제목/링크/테두리 등 강조 요소에만 쓰고, 본문 문단은 연한 라벤더 화이트(`--text`)로 유지해 긴 글도 읽기 편하게 함.
- **글로우 토글**: 헤더의 `#theme-toggle` 버튼은 라이트/다크 전환이 아니라 네온 글로우 효과 on/off 토글 (`html.no-glow` 클래스, `localStorage.glow`). 새 강조 요소를 추가할 때 글로우 스타일이 `.no-glow`에서도 깨지지 않는지 확인.
- **카드형 그리드 레이아웃**: 글 목록은 `.post-list`에 CSS Grid로 카드 배치 (모바일 1열 → `min-width: 700px`부터 2열). 새 목록형 UI를 추가할 때도 이 그리드 패턴을 따름.
- **모바일 반응형**: 모바일 퍼스트로 작성, 주요 브레이크포인트에서 레이아웃 확인 (좁은 화면에서 헤더/그리드 등이 무너지지 않도록).

## 예상 구조 (참고용, 실제 구현 시 조정 가능)

```
/posts/          마크다운 글 파일들 (.md)
/index.html      글 목록 페이지
/post.html       글 본문 페이지 (쿼리 파라미터 등으로 특정 글 로드)
/css/            스타일시트
/js/             마크다운 파서, 렌더링 로직, 다크모드 토글 등
```

## 작업 시 참고사항

- 새 기능을 추가할 때 프레임워크나 빌드 도구를 끌어오려는 유혹이 있다면 먼저 순수 JS로 가능한지 확인.
- 새 UI 요소를 추가할 때 네온 글로우 on/off 두 상태 모두, 그리고 모바일 화면에서의 표시를 함께 확인.
